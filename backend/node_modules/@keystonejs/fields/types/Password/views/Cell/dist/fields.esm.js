import { jsx } from '@emotion/core';
import { colors } from '@arch-ui/theme';

/** @jsx jsx */

const PasswordCellView = ({
  item: {
    password_is_set
  } = {}
}) => {
  return password_is_set ? 'Is set' : jsx("span", {
    css: {
      color: colors.danger,
      fontWeight: 'bold'
    }
  }, "Not set");
};

export default PasswordCellView;
