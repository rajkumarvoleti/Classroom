'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _defineProperty = require('@babel/runtime/helpers/defineProperty');
var dateFns = require('date-fns');
var Controller_dist_fields = require('../../../../../Controller/dist/fields.cjs.prod.js');
require('@babel/runtime/helpers/objectWithoutProperties');
require('@babel/runtime/helpers/classCallCheck');
require('lodash.isequal');

class DateTimeController extends Controller_dist_fields['default'] {
  constructor(...args) {
    super(...args);

    _defineProperty(this, "getFilterGraphQL", ({
      type,
      value
    }) => {
      const key = type === 'is' ? `${this.path}` : `${this.path}_${type}`;
      return {
        [key]: value
      };
    });

    _defineProperty(this, "getFilterLabel", ({
      label
    }) => {
      return `${this.label} ${label.toLowerCase()}`;
    });

    _defineProperty(this, "formatFilter", ({
      label,
      value
    }) => {
      const formatConfig = this.config.format;
      let formattedValue = value;

      if (formatConfig) {
        formattedValue = dateFns.format(dateFns.parseISO(value), formatConfig);
      }

      return `${this.getFilterLabel({
        label
      })}: "${formattedValue}"`;
    });

    _defineProperty(this, "serialize", data => {
      let value = data[this.path];

      if (typeof value !== 'string') {
        return null;
      }

      return value.trim() || null;
    });

    _defineProperty(this, "getFilterTypes", () => [{
      type: 'is',
      label: 'Is exactly',
      getInitialValue: () => ''
    }, {
      type: 'not',
      label: 'Is not exactly',
      getInitialValue: () => ''
    }, {
      type: 'gt',
      label: 'Is after',
      getInitialValue: () => ''
    }, {
      type: 'lt',
      label: 'Is before',
      getInitialValue: () => ''
    }, {
      type: 'gte',
      label: 'Is after or equal to',
      getInitialValue: () => ''
    }, {
      type: 'lte',
      label: 'Is before or equal to',
      getInitialValue: () => ''
    } // QUESTION: should we support "in" and "not_in" filters for DateTime?
    // What does the UI look like for that.
    ]);
  }

}

exports.default = DateTimeController;
