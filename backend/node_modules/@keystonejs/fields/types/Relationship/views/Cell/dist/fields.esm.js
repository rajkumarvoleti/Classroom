import React, { Fragment } from 'react';

var Cell = (({
  data,
  field,
  Link
}) => {
  if (!data) {
    return null;
  }

  const {
    fullPath
  } = field.getRefList();
  return /*#__PURE__*/React.createElement(Fragment, null, (Array.isArray(data) ? data : [data]).filter(item => item).map((item, index) => /*#__PURE__*/React.createElement(Fragment, {
    key: item.id
  }, !!index ? ', ' : '', /*#__PURE__*/React.createElement(Link, {
    path: fullPath,
    item: item
  }, item._label_))));
});

export default Cell;
