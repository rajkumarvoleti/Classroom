import React from 'react';
import { R as RelationshipSelect } from '../../../../../dist/RelationshipSelect-ec1cacc8.esm.js';
import '@babel/runtime/helpers/extends';
import '@babel/runtime/helpers/objectSpread2';
import '@babel/runtime/helpers/objectWithoutProperties';
import '@emotion/core';
import '@apollo/client';
import '@arch-ui/select';
import 'react-select';
import 'intersection-observer';

const EventCatcher = ({
  children
}) => /*#__PURE__*/React.createElement("div", {
  onClick: e => {
    e.preventDefault();
    e.stopPropagation();
  }
}, children);

const RelationshipFilterView = ({
  onChange,
  filter,
  field,
  value
}) => {
  const handleChange = option => {
    if (option === null) {
      onChange(null);
    } else {
      const {
        value
      } = option;

      if (value) {
        onChange(value.id);
      }
    }
  };

  if (!filter) return null;
  const htmlID = `ks-input-${field.path}`;
  return /*#__PURE__*/React.createElement(EventCatcher, null, /*#__PURE__*/React.createElement(RelationshipSelect, {
    field: field,
    renderContext: null,
    htmlID: htmlID,
    onChange: handleChange,
    value: value,
    isMulti: false
  }));
};

export default RelationshipFilterView;
