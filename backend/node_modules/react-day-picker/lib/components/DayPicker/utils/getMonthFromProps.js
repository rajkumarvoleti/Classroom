import { isAfter, isBefore, startOfMonth } from 'date-fns';
export function getMonthFromProps(props) {
    var _a, _b, _c;
    var toMonth = props.toMonth, fromMonth = props.fromMonth;
    var month = (_c = (_b = (_a = props.initialMonth) !== null && _a !== void 0 ? _a : props.month) !== null && _b !== void 0 ? _b : (props.today !== 'off' ? props.today : undefined)) !== null && _c !== void 0 ? _c : new Date();
    month = startOfMonth(month);
    // Fix initial month if is after the `toMonth` prop.
    if (toMonth && isAfter(month, startOfMonth(toMonth))) {
        month = startOfMonth(toMonth);
    }
    // Fix initial month if before the `fromMonth`
    if (fromMonth && isBefore(month, startOfMonth(fromMonth))) {
        month = startOfMonth(fromMonth);
    }
    return month;
}
//# sourceMappingURL=getMonthFromProps.js.map