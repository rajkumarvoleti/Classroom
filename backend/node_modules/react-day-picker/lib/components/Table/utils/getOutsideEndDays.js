import { addDays, differenceInDays, endOfWeek } from 'date-fns';
export function getOutsideEndDays(month, props) {
    var locale = props.locale;
    var days = [];
    var lastDayOfWeek = endOfWeek(month, { locale: locale });
    var endDiff = differenceInDays(lastDayOfWeek, month);
    for (var i = 1; i <= endDiff; i++) {
        var dayDate = addDays(month, i);
        days.push(dayDate);
    }
    return days;
}
//# sourceMappingURL=getOutsideEndDays.js.map