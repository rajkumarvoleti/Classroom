import { addDays, differenceInDays, startOfWeek } from 'date-fns';
export function getOutsideStartDays(month, props) {
    var locale = props.locale;
    var days = [];
    var firstDayOfWeek = startOfWeek(month, { locale: locale });
    var startDiff = differenceInDays(month, firstDayOfWeek);
    for (var i = 0; i < startDiff; i++) {
        var newDay = addDays(firstDayOfWeek, i);
        days.push(newDay);
    }
    return days;
}
//# sourceMappingURL=getOutsideStartDays.js.map