import { DayPickerProps } from 'types';
/**
 * Returns the next and the previous months that the user can navigate to.
 */
export declare function getPrevNextMonths(props: DayPickerProps): [Date?, Date?];
