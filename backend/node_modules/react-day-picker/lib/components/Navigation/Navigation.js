var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { getNavigationComponent } from './getNavigationComponent';
export function Navigation(props) {
    var dayPickerProps = props.dayPickerProps;
    var _a = getNavigationComponent(dayPickerProps), rootProps = _a.rootProps, nextButtonProps = _a.nextButtonProps, prevButtonProps = _a.prevButtonProps;
    var _b = dayPickerProps.components, PrevIcon = _b.PrevIcon, NextIcon = _b.NextIcon;
    var prevButton = (React.createElement("button", __assign({}, prevButtonProps, { key: "prev" }),
        React.createElement(PrevIcon, null)));
    var nextButton = (React.createElement("button", __assign({}, nextButtonProps, { key: "next" }),
        React.createElement(NextIcon, null)));
    var buttons = [prevButton, nextButton];
    if (dayPickerProps.dir === 'rtl') {
        buttons = buttons.reverse();
    }
    return React.createElement("div", __assign({}, rootProps), buttons);
}
//# sourceMappingURL=Navigation.js.map