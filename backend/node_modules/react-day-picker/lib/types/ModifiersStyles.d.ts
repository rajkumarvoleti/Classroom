/// <reference types="react" />
import { Modifier } from './Modifier';
/**
 * Represent the inline-style assigned to each modifier.
 */
export declare type ModifiersStyles = {
    [modifier in Modifier]: React.CSSProperties;
};
