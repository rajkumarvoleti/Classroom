import { GeneratorOptions, GeneratorManifest, BinaryPaths } from '@prisma/generator-helper';
export declare class Generator {
    private generatorProcess;
    manifest: GeneratorManifest | null;
    options?: GeneratorOptions;
    constructor(executablePath: string, isNode?: boolean);
    init(): Promise<void>;
    stop(): void;
    generate(): Promise<any>;
    setOptions(options: GeneratorOptions): void;
    setBinaryPaths(binaryPaths: BinaryPaths): void;
}
