/// <reference types="node" />
import { Client, Pool } from 'undici';
import { URL } from 'url';
export declare class Undici {
    private pool;
    private closed;
    constructor(url: string | URL, moreArgs?: Pool.Options);
    request(body: Client.DispatchOptions['body'], customHeaders?: Record<string, string>): Promise<unknown>;
    status(): Promise<unknown>;
    close(): void;
}
